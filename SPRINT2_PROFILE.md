# Sprint 2 — Profile (Supabase)

## SQL (chạy trên Supabase SQL Editor)

1. Đã có `schema.sql` → chạy `supabase/sprint2_profile.sql`

## Tính năng app

- **Cá nhân** → thống kê followers/following (nếu đã chạy SQL)
- **Chỉnh sửa hồ sơ** → họ tên, bio, URL avatar
- Tự refresh profile khi quay lại tab Cá nhân

## Chạy lại app sau sửa .env

```powershell
npx expo start -c
```

URL đúng dạng: `https://sxyjmynwwqzrgedybrai.supabase.co`
