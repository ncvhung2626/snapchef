# Deprecated — use main seed

Run **`supabase/dev_seed.sql`** instead (schema-validated against `supabase/SCHEMA_AUDIT.md`).

This file is kept for reference only and may reference columns that do not match your database.
